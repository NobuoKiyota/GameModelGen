# AnimeFace 現在の状態

**更新**: 2026-09-21

| 項目 | 内容 |
| :--- | :--- |
| 現在の工程 | 顔（目から作るアニメ顔） |
| 完了 | **S01** 下絵・カメラ配置（13/13 PASS）／**S02** 目の初期ループ — 人間が頂点を追加して補正（`out/face_s02_human.blend`）→ **S02b** で全面四角形に整形（7/7 PASS、`out/face_s02b.blend`：26頂点・13四角面） |
| 完了（続き） | **S03** 口のループ＋Mirror（19/19 PASS、`out/face_s03_gen.blend`）。半ループ7頂点×内外＋6四角面、Mirror(X, Clipping, Merge)。Mirror 後 76頂点・114辺・38面。**手直しなしで承認済み** |
| 完了（続き） | **S04** 鼻筋のライン（20/20 PASS、`out/face_s04_gen.blend`）。目頭の頂点→鼻根→鼻筋→鼻先→鼻下（頂点4・辺4、面なし、X=0 に吸着）。**確認済み（手直しなし）** |
| 完了（続き） | **S05** 頬から顎先へのフェイスライン（20/20 PASS、`out/face_s05_gen.blend`）。目の下の7頂点の列→中間の列→顎の列、四角面12枚。Mirror 後 107頂点・62面・辺171。**ユーザーが中間の列を仮調整（`out/face_s05_human.blend`。`face_s05_gen_usert.blend` の複製）**。顎の列は無変更 |
| 完了（続き） | **S06** 頬・鼻・口の面張り＋鼻・口まわりの頂点追加（32/32 PASS、`out/face_s06_gen.blend`）。格子3列×7行、正中線に頂点4、口の輪の前後を輪郭に合わせ直し。Mirror 後 225頂点・411辺・184面。**要手直し**: 口の端〜顎先の楔形、頬の帯の目頭側の針状の列。**ユーザーが位置を手直し済み（`out/face_s06_human.blend`＝`face_s06_gen_human.blend` の複製。トポロジー同一・45頂点を移動）** |
| 完了（続き） | **S07** 顔のマスクを後ろへ広げる（25/25 PASS、`out/face_s07_gen.blend`）。外側のライン15頂点を後ろへ押し出し（四角面14枚）、おでこのラインを放物線に。Mirror 後 254頂点・468辺・212面。**ユーザーが顎の下の後ろ側を微調整済み（`out/face_s07_human.blend`）。字幕 14:14〜15:00（マスクの延長・額の面）は S07b で実装** |
| 完了（続き） | **S07b** 額の面（23/23 PASS、`out/face_s07b_gen.blend`）。下辺12頂点→上端（Z=+0.19）まで4段・四角面44枚。Mirror 後 346頂点・648辺・300面。**ユーザーが額の段差を微調整済み（`out/face_s07b_human.blend`、12頂点を移動）** |
| 完了（続き） | **S08** Subsurf（ビューポート1・レンダー2）＋鼻先・顎先のサポートループ（23/23 PASS、`out/face_s08_gen.blend`）。ソース230頂点・Mirror 後ケージ 440頂点/388面。鼻先 +0.5mm・顎先 斜めへ +8.9mm を補正（**要確認**）。**ユーザーの確認待ち** |
| 完了（続き） | **S09a** 頭のガイド＋頭の上から後ろへの弧（21/21 PASS、`out/face_s09a_gen.blend`）。ガイド楕円体（ワイヤー）、額の上端の一番前3頂点から 13段×3列・四角面26枚。Mirror 後 505頂点・947辺・440面。**ユーザーの確認待ち**（弧の後ろの端 Z=-0.03 は「この辺まで」に基づく私の判断） |
| 完了（続き） | **S09b** 頭の側面の穴埋め（29/29 PASS、`out/face_s09b_gen.blend`）。格子10列×15行（面139枚）＋弧の行2＋下の縁。Mirror 後 781頂点・1513辺・726面。**T_12 の凹んだ角で折れた面11枚を削除し、小穴（各側 8頂点・14頂点）を残した**。四角形の内角が25°〜155°の外: 15/139。**ユーザーの確認待ち** |
| 完了（続き） | **S09b の人間補正版** `out/face_s09b_human.blend`（T_12 の穴を埋めた。三角形2枚・五角形2枚が混在）→ **S09b2**（17/17 PASS、`out/face_s09b2_gen.blend`）: 五角形2枚を四角形3枚に切り直し（頂点は動かさない）。三角形2枚は折れなしには動かせないので残す（面 361・四角形359）。Mirror 後 757頂点・1483辺・722面 |
| 完了（続き） | **S09b2 の人間補正版** `out/face_s09b2_human.blend`: 三角形2枚を五角形6枚に変えた（額の上端に頂点6追加・壁ぎわに1追加・3点移動）。ただし壁ぎわに**内向きの三角形1枚と、孤立した頂点2・辺2が残っていた**（辺2本が3枚共有＝非多様体）→ **S09b3**（18/18 PASS、`out/face_s09b3_gen.blend`）: その面・辺・頂点だけを削除（頂点の位置は変えない）。**三角形0・五角形6・四角形357**（面363、頂点402、辺765）。Mirror 後 769頂点・1497辺・726面 |
| 完了（続き） | **S09c**（33/33 PASS、`out/face_s09c_gen.blend`、ユーザーの確認待ち）: 下の穴（36辺）に輪 R1（L から首の輪へ半分寄せ＋外側6mm）を足し、首の輪（12頂点、中心 Y=0.006、半径 0.058、Z -0.196〜-0.148）へ四角形だけで絞った。頂点+26・面+30（面393＝五角形6・四角形387）。Mirror 後 817頂点・1605辺・786面。穴は目2・口・首（12辺）の4つだけ。へこみ2頂点（381, 393）を外側へ寄せた（最大8mm以下）。**首の左右の幅は下絵にない（円と仮定）**。壁の下端の折れ角 62°（要確認） |
| 完了（続き） | **S09c は、ユーザーが手直し不要と確認**（`face_s09c_human.blend` は生成版と位置の差 0）→ **S10**（27/27 PASS、`out/face_s10_gen.blend`、ユーザーの確認待ち）: 首の円柱（12頂点・蓋なし）を、S09c の首の穴の輪を根元として追加（頂点+35・面+30、面423）。輪5本（側面下絵の行 830/862/894/926/958、水平。前後の幅 128→199mm で、円と仮定）。下端は蓋なしの12辺の穴。Mirror 後 877頂点・1725辺・846面。首の前後の線は、下絵と ±2px。HeadGuide を削除。**顎・口まわりの輪郭は、Subsurf 後に下絵より前（行680〜760で +10〜21px、行820で +17px）**（S06 の口の下の楔＋S08 の顎先の補正。要確認） |
| 完了（続き） | **S10 は、ユーザーが直し無しと確認** → **S11**（22/22 PASS、`out/face_s11_gen.blend`、ユーザーの確認待ち）: 目の内側（眼窩）。縁の13頂点から、縁に接する面の法線の逆向きへ、R1（面内 1.10倍・奥へ 14mm）→ R2（0.55倍・奥へ 24mm）→ 中心（奥へ 30mm）。壁13＋13枚と、底は四角形6＋三角形1（13頂点は四角形だけにできない。眼球の裏で見えない）。縁 R0 と R1 の26辺にクリース1.0・シャープの印、全ての面をスムースシェード。頂点+27・面+33（面456）。Mirror 後 931頂点・1843辺・912面。穴は口（16）と首の下端（12）だけ。**奥行き・大きさの数値は仮定（下絵にない）**。**目の上下の頂点数（上6・下5）は揃えていない**（全て四角形のままでは偶奇が合わず、輪を遠くまで伸ばす必要がある。目を閉じる形を作るときに扱う） |
| 完了（続き） | **S11 の人間補正版** `out/face_s11_human.blend`: 顎の下・壁の下端の頂点41個を最大 11.7mm 動かし、顎の下（首の前）に頂点5つを足した（五角形+1、面460）。眼窩・縁の頂点は動かしていない。ただし口の下の正中線の頂点2つが X=+0.0015 にずれ、Mirror で結合されず、口の穴が16→20辺（切れ目）になっていた → **S11b**（15/15 PASS、`out/face_s11b_gen.blend`）: 正中線がずれた8頂点だけを X=0 に戻した。Mirror 後 940頂点・1860辺・920面、穴は口16・首12。クリース・シャープは26辺のまま |
| 完了（続き） | **S12**（28/28 PASS、`out/face_s12_gen.blend`、ユーザーの確認待ち）: 口の中の空洞（動画 33:14〜36:17）。口の縁（半分で9頂点・8辺）の8辺にクリース1.0・シャープの印（眼の26辺と合わせて34辺）。R1: 上唇と下唇の頂点の対の中点から上下の幅を1.3倍・奥10mm（クリース・シャープなし）、R2: 0.6倍・奥20mm。底は四角形3枚＋口角の三角形1枚。頂点+18・面+20（面480）。Mirror 後 972頂点・1931辺・960面。穴は首の下端（12辺）だけになった（頭が閉じた）。**大きさ・奥行きは仮定**（下絵にない。動画も「口の中は形にこだわらない」）。**顔の外形は、動画 36:08 の「大体できた」の段階に到達** |
| 完了（続き） | **S12 の人間版** `out/face_s12_human.blend`: 口の中の頂点（R1・R2・口角の1点）の位置だけを整理。トポロジー・クリース・シャープは同じ。Mirror 後は問題なし（穴は首の下端12辺のみ）→ **これを次の入力とする**。**過剰なトポロジーの調査（読み取り専用、`out/s13_loops_analysis.json`・`s13_trials.json`）**: 頂点513・面480（半分）。保護（目・口・鼻・クリース）の外で、ループを消しても三角形・五角形が増えないのは 57（耳の壁、4面、限界曲面の差 0.72mm）・92（同、4面、1.40mm）・127（首の輪、6面、0.89mm）・88（頭の後ろ、14面、3.95mm）ほか。面の平均化は、移動を 4mm 以下に抑えて、辺の長さのばらつき（CV）が 0.473→0.447、限界曲面の差の最大 0.98mm。**どちらも効果は小さい**（ユーザーの判断待ち） |
| 完了（続き） | **後編（動画『顔のモデリング（後編）』`.agents/handoffs/fusako_02_subtitles_*`）S13**（21/21 PASS、`out/face_s13_gen.blend`、ユーザーの確認待ち）: 瞳（黒目）を別オブジェクト `Eye` で作成（動画 00:00〜02:36）。下絵3枚（正面・45°・側面）の黒目の輪郭に楕円を当てはめ（`lib/measure.py: iris_ellipse`）、3次元の黒目（中心 (-0.1134, -0.1609, 0.0019)、外向き 16.7°・下向き 15.4°、半径 横38.3mm・縦49.4mm）に当てはめた。正面・側面の誤差は 3px 以内（45° は半径のみ重み 0.5、中心は使わない）。凹んだ円盤（中心を縁の平面より 8mm 奥へ）: 極の三角形32＋四角形128（161頂点・160面）、Mirror（Mirror Object = Face）のみ、Subsurf なし、スムースシェード。Mirror 後 322頂点・320面。皮膚と交差せず、最小距離 4.0mm。**凹み 8mm は仮定**。**黒目は開口（縁の輪）より上下に大きく、26/161 頂点が開口の外**（S14 のまつ毛・下まぶたで覆う）。**眼窩（S11）の底は、黒目の中心より奥（約 22mm）**（動画は浅め。浅くするなら S11 の R2・中心を前へ） |
| 完了（続き） | **S13 の人間版** `out/face_s13_human.blend`: `Eye` だけを動かした（重心で 下へ 7.2mm・後ろへ 6.2mm）。顔は同じ → **これを入力とする** → **S14**（24/24 PASS、`out/face_s14_gen.blend`、ユーザーの確認待ち）: 上まつ毛（太いほう、動画 02:36〜07:29・10:37〜11:39）を別オブジェクト `Eyelash` で作成。基準の線は目の縁 R0 の上側 8頂点（内眼角→外眼角）。下絵の黒い帯の外向きの厚み（3〜22px）と、跳ね上げの先端（(220, 492.5)px）を測り、皮膚の表面へ光線を当てて外側の点 O、内側の点 I（4px）、ひし形の断面 O・F（前の稜 +2mm）・I・K（奥 0.4mm）。駅は間を補って18（17区間）、頂点72・面70（側面4×17＋端2）。Mirror（Mirror Object = Face）＋ Subdivision（表示1・レンダー2）、前の稜17辺にクリース1.0、スムースシェード。断面の幅の補正 kf=1.6（Subsurf で 2/3 に縮むため）で、Subsurf 後の正面の投影と下絵の帯の IoU 0.87（引き出し後 0.75）。**黒目の上端が皮膚より手前に出ているため、まつ毛を皮膚の法線方向へ最大 7.5mm 出さないと交差する**（黒目を奥へ 3mm 動かすと 3.25mm に減る）。**細いまつ毛（動画 07:43〜10:00）は S14b** |
| 完了（続き） | **S14 の人間版** `out/face_s14_human.blend`: まつ毛のリング1〜13を下絵に合わせて動かし、外眼角に、跳ね上げの先端（(-213.8,-69.6,20.1)mm）と、下まぶたへ落ちる刃（先 (-162.6,-130.7,-38.7)mm）を作った（頂点63・面59、全て四角形）。端が、長さ 45〜66mm・幅ほぼ 0 の細長い面、価数2の頂点2、開いた端（境界の辺6）、内眼角の針（v2）になっていた → **S14c**（19/19 PASS、`out/face_s14c_gen.blend`、ユーザーの確認待ち）: 端だけをつなぎ直した（リング1〜12は人間版のまま）。内眼角: 小さい輪＋針の先の輪。外眼角: リング12から先端へ向かう途中に輪を置き、3区間で細くして閉じる。刃: 別の閉じた細い管（動画 09:40 の「ぶっ刺す」と同じ）。頂点88・面84（主管72頂点＋刃16頂点、全て四角形、閉じた2成分）。**Subsurf 後の正面の投影が、人間版と IoU 0.82（内眼角 0.80・外眼角 0.71）**、側面 0.78。自己交差 0、顔との交差 10（人間版 22）、黒目との交差 0。面の縦横比の最大 16（人間版 14）。**人間の手作りの形（特に外眼角）と、完全には一致しない** |
| 完了（続き） | **S14b**（18/18 PASS、`out/face_s14b_gen.blend`、ユーザーの確認待ち）: 細いまつ毛の房（動画 07:43〜10:00）。**1回目の生成（3輪・直線2本、リング7/9/11）は、まっすぐな針のように見え、ユーザーに指摘された** → 作り直し: 断面5輪（根元〜先端）を2次ベジエでなだらかに曲げ、根元から先端へ滑らかに細くする。房どうしが重ならないよう、リング4・9の2か所に。大きさ（長さ・太さ・くるみ＝長さに対する割合）は、主まつ毛・顔・黒目のどれとも交差しない組み合わせを探索（見つからなければ作らない）。頂点+40・面+36（房2つ、各20頂点・18面）。前の稜（房ごと3辺）にクリース1.0。Mirror 後 256頂点・480辺・240面。**房の位置・大きさは下絵から厳密に測っていない**（動画も「だいたいの位置」） |
| 完了（続き） | **S15**（23/23 PASS、`out/face_s15_gen.blend`、ユーザーの確認待ち）: 眉（`Eyebrow`）と二重線（`DoubleLid`）（動画 12:01〜13:50）。S14 の主まつ毛と同じ、実測にもとづく帯（房の「見た目まかせの曲げ」は使わない）。眉は正面の下絵の茶色い塗りの上端・下端を188列で測り、20駅の帯（頂点156・面154）。二重線は下絵の薄い線の中心を138列で測り、一定の半幅1.3mmの16駅の帯（頂点124・面122）。どちらも、外側・前の稜・内側・後ろのひし形断面、前の稜にクリース1.0、Mirror(Object=Face)+Subdivision(1/2)。顔の面と交差なし（最小距離: 眉0.25mm・二重線0.49mm）、自己交差なし。眉のIoU（Subsurf後正面投影 対 下絵の測定領域）0.55。**側面は正面の測定だけから作っており、下絵の側面の眉とは厳密には合っていない**。**眉尻（内側）で帯が下絵の輪郭より少しはみ出す** |
| 完了（続き） | **S15b**（20/21 PASS、`out/face_s15b_gen.blend`、ユーザーの確認待ち）: 房（`Eyelash.001/.002/.003`）を、動画のように外眼角側1か所へまとめ直した（ユーザーの指摘）。各房の頂点の相対位置は変えず、オブジェクトの位置・回転だけを変更（根元の輪を、主まつ毛の頂点57・65・69の付近へ、向きを-12°/0°/+14°ずらして再配置）。房どうしの交差は0。`Eyelash.001` の自己交差2枚は、S14b2 で報告した既存のねじれで、今回の移動とは無関係 |
| 完了（続き） | **S15 を作り直した**（ユーザー指摘: 眉がギザギザ・眉と二重線が接触）。25/25 PASS、`out/face_s15_gen.blend`（上書き、ユーザーの確認待ち）。原因: ①下絵の画素単位のがたつき（標準偏差1.8px・最大7px）をそのまま形にしていた → 移動平均で均し、駅を20→13に。②二重線の測定範囲が、内側の端で眉の下端の画素と混じっていた → 範囲を絞り、眉と二重線の最小距離のゲートを追加（31.0mm）。③駅を密にする補間数を増やした際、太さの割り当てがずれるバグ → 修正（IoU 0.55→0.57）。**S15b（房の再配置）は不採用に**（ユーザーが `face_s15_human.blend` へ戻すと判断。房は S14b2 の手直し済みの位置のまま、今回のS15再生成でも変えていない） |
| 完了（続き） | **S15c を作り直した**（27/27 PASS、`out/face_s15c_gen.blend`、ユーザーの確認待ち）: `DoubleLid` にオブジェクト単位の移動・拡大（X×1.206）が付いており、頂点ローカル座標のまま顔（ワールド座標）と比較していたため、「顔に近づいたら押し戻す」処理が座標系のずれで暴走し、頂点が飛んだ（ユーザー報告「ぐちゃぐちゃ」）。**修正**: ①オブジェクトの変換をメッシュへ焼き込んでから処理 ②押し戻し処理をやめ、間引く前（承認済みの元の形）自身の「顔との三角形の重なり数」を基準にし、間引いた後もそれ以下であることを確認する方式に変更。眉196→32頂点（顔との重なり0=元と同じ）、二重線68→28頂点（重なり10、元は12）。両方とも自己交差0、眉と二重線の距離27.8mm |
| 完了（続き） | **S16 を作り直した**（ユーザー指摘: 動画は前後・首でシーム分割しているのに、円柱1枚投影のままだった）。58/58 PASS、`out/face_s16_gen.blend`（上書き、ユーザーの確認待ち）。`Face` を、動画14:12と同じ①前②後ろ③首（Z<-0.19）の3つの島に分け、前後は正面/背面の平面投影、首は円柱投影。**1回目は頂点ごとに島を決め、区画をまたぐ面ができてUVが画面全体に伸びる線になった → 面（ポリゴン）ごとに島を決め、その面の4隅を同じ島の式で計算する方式に修正**。他（帯状オブジェクト・黒目・配置・テクスチャ）は変更なし |
| 完了（続き） | **S16 を再度作り直した**（ユーザー指摘: UVエディタで前後の島にまだ大きな引き伸ばし（耳前あたりの面が混入した三角の張り出し）があり、パーツ分けが不十分に見える）。58/58 PASS、`out/face_s16_gen.blend`（上書き、ユーザーの確認待ち）。原因: 前後の境界を座標 Y=0 の平面で切っていたため、耳の前あたりの側面向きの面まで「前」に混ざり、平面投影で大きく引き伸ばされていた。**修正**: 境界を座標ではなく面法線のY成分の符号（=輪郭線＝法線が真横を向く場所）で決める方式に変更。ワイヤーフレーム画像（`out/s16_face_uv_wire2.png`）で確認、耳の張り出しが消え、前・後とも滑らかな輪郭になった（後ろの島には耳の穴も同心円状にきれいに出ている） |
| 完了（続き） | **S17**（15/15 PASS、`out/face_s17_gen.blend`、ユーザーの確認待ち）: ベースカラーの塗り分け（動画18:11〜23:49の一部）。下絵に色見本が無いため配色は暫定（人間が後で調整する前提）。`bpy.ops.paint`系はUV演算子と同様`--background`で機能しない前提で、S16のUVからテクスチャへ直接ピクセルを書き込む方式。肌全面（SKIN）→白目20面（Face、index<460かつ全頂点index≥463で選別）→口の中20面（Face、index460-479）→眉30面+二重線26面（紫系BROW）→まつ毛4オブジェクト計114面（LASH）→黒目160面（IRIS、上側にIRIS_SHADOWの柔らかいグラデーション）→頬（3D座標で選んだ29面に、距離に応じた柔らかいBLUSH重ね）→鼻先（頂点index42近傍に同様の柔らかい重ね）の順。首下・顎の境目の塗り分けは効果が小さいため見送り（保留）。レビュー: `out/s17_render_front.png`・`s17_render_45.png`・`s17_render_side.png`（一時的にSun light+Worldを追加してEeveeでレンダリング。.blendには保存していない）。目視で色の混入なし、歪みなし |
| 完了（続き） | **S17 手直し**（6/6 PASS、`out/face_s17_gen.blend`上書き、ユーザーの確認待ち）: ユーザーが `face_s17_human.blend` で①眉をY方向へ26.8mm手前へ移動（「埋没」＝皮膚に埋まって見えていたのを解消）②「白目が目頭まで大きく浸食している」と指摘。①は保持したまま、②を修正: 眼窩内側20面のうち目頭寄り（Xが0に近い）9面を、白から目頭寄りの柔らかい暖色（涙丘に近いトーン）へなだらかにブレンド。外側の面は白のまま。`out/s17fix_render_front.png`・`s17fix_render_45.png`で目視確認 |
| 完了（続き） | **S17 手直し2**（4/4 PASS、`out/face_s17_gen.blend`上書き、ユーザーの確認待ち）: ユーザーが画像で指摘（目尻付近の黒い塊・多角形の透け）。BVHTreeでワールド座標の重なりを調査し、`Eyelash.001`（全10面が主まつ毛・顔と重複）と`Eyelash.003`（オブジェクトスケールが1.6778倍になっており、11面が主まつ毛、6面が顔と重複）が、根元が皮膚・主まつ毛に埋まっていたことが判明（`Eyelash.002`は重なりなし）。**修正**: 各房の変換をメッシュへ焼き込んで単位行列に戻し、根元近傍の顔の法線方向（皮膚の外向き）へ房全体を剛体的に押し出す候補探索（Eyelash.001は12mm、Eyelash.003は10mm）で、顔・主まつ毛のどちらとも重ならなくなる最小の押し出しを採用。`out/s17fix2_render_front.png`・`s17fix2_render_45.png`で黒い塊・透けが消えたことを目視確認 |
| 完了（続き） | **S16 を3度目に作り直した**（ユーザー指摘: オレンジ枠で囲んだ画像2枚。①目の周りを塗ると関係ない場所も変わる／目の内と外側が一緒になっている ②口の周りで口内の色が多角形状ににじみ出て見える）。原因: 眼窩内側（白目）・口の中の面が、奥（-Y）へ折り返しているのに、外側の皮膚と同じ単純な(X,Z)平面投影を使っていたため、頬・顎などの外側の面とほぼ同じUV画素を共有していた（実際に測定: UV距離が0.0006〜0.02、2048px換算で1〜40px という近さで重複）。**修正**: 眼窩内側・口の中を、front/back/neckとは別の独立した2つの島（`eye_socket`・`mouth`）に分離。58/58 PASS、`out/face_s16_gen.blend`（上書き）。これに伴いS17をやり直した（下の行） |
| 完了（続き） | **S17を作り直し、既存の3つの手直し（眉のY方向オフセット・白目目頭側のグラデーション・房の押し出し）を再適用**（`s17_reapply_fixes.py`）。15/15 + 5/5 PASS、`out/face_s17_gen.blend`（上書き）。`out/s17reapply_render_{front,45,side}.png`で、口周りのにじみ出し・目周りの色混入が消えたことを目視確認 |
| 完了（続き） | **S17b**（12/12 PASS、`out/face_s17b_gen.blend`、ユーザーの確認待ち）: 目のハイライト（動画24:09〜27:41）。`Eye`オブジェクトの全面を複製し、法線方向へ0.4mm押し出して同じメッシュに追加。複製面のUVを、既存8オブジェクトと被らない空き区画（自動探索）へ再配置し、その区画を基本アルファ0（透明）、白い丸2つ（大小、黒目の左上寄り）だけアルファ1で塗った。共有テクスチャにアルファチャンネルを追加したが、**共有マテリアル自体は`HASHED`のまま変更せず**、複製面だけに使う別マテリアル（`HASHED`）を新設してEyeの2枚目のマテリアルスロットに割り当てた（共有マテリアルを`BLEND`にしたところ頭頂部が不自然に光る不具合が出たため、影響範囲を複製面だけに限定）。**実装中に見つけたバグ2つ**: ①bmeshで新しい面を複製する際、UVは自動で引き継がれない（既定で0,0になる）ため、複製面が全てテクスチャの同じ1点をサンプリングしてしまい、ハイライトの丸がいくら大きくても全く見えない → 元の面からUVを明示的にコピーして解決。②**`BaseColor`画像がS16で`alpha=False`（24bit、実質アルファ無し）で作られていたため、アルファを書き込んでも保存後は必ず1.0に戻ってしまい、ハイライトの透明部分が不透明に描画され黒目全体が白っぽく見える不具合**（自分の検証コードが、書き込み直後の自分のnumpy配列を読んでいて保存後の実データを確認していなかったため、最初は気づけなかった。保存→再読込→黒目だけを単独でレンダーする、という切り分けで発見）→ アルファ有り（32bit）の画像を作り直し、全マテリアルのノードを付け替えて解決 |
| 完了（続き） | **S17c**（6/6 PASS、`out/face_s17c_gen.blend`、ユーザーの確認待ち）: 主まつ毛に「切れ込み」を1つ追加（動画30:18〜30:56）。22駅中、駅13の前稜(F)を内側(I)へ40%寄せて凹ませ、同じ見た目を頂点数・UVを変えずに実現。顔・黒目・他の房との重なりが増えていないこと、自己交差が増えていないこと（間引き前の基準値と比較）を確認 |
| 完了（続き） | **ユーザーが `face_s17c_human.blend` で「角ばっていた」頂点を手直し**（Eyelash35頂点・Face66頂点、最大21.5mm、頂点・面数は不変）。これを入力にS17dを実施 |
| 完了（続き） | **S17d**（9/9 PASS、`out/face_s17d_gen.blend`、ユーザーの確認待ち）: 口の形調整（動画28:31〜29:54）。オメガ形の細部は下絵に無く判断材料が薄いため見送り（保留）。口の縁9頂点＋口の中18頂点（S12で確定したindexで選別。座標のしきい値ではないので、人間の手直しで座標が変わっても安定）を、上+手前へ1.0/2.5mm移動し、X方向を0.85倍に縮小（半分メッシュなので中心線に寄る＝幅が狭くなる）。自己交差・他オブジェクトとの重なりが増えていないことを確認 |
| 完了（続き） | **S17e**（10/10 PASS、`out/face_s17e_gen.blend`、ユーザーの確認待ち）: オブジェクト整理（動画31:00〜32:31）。主まつ毛(`Eyelash`)を`Face`にJoin（Ctrl+J相当、`Face`をアクティブにして実行。モディファイア・名前は`Face`のまま）。頂点601（513+88）・面564（480+84）で一致、モディファイアはMirror→Subdivision(1/2)のまま、マテリアルは共有の`CharacterBase`1枚のまま。コレクション（REF=下絵・カメラ／MODEL=作業対象／GUIDE）はS01で既に分離済みのため変更不要（確認のみ）。Eye・Eyebrow・DoubleLid・房3つは別オブジェクトのまま（動画通り） |
| 完了（続き） | **S17f**（6/6 PASS、`out/face_s17e_gen.blend`上書き、ユーザーの確認待ち）: ユーザーから「アルファ不具合の修正を反映して、目のハイライトを塗り直して」との依頼。確認したところ、アルファ不具合の修正は`face_s17b_gen.blend`をS17cの入力に切り替えた時点で既にS17c〜S17eの系列に反映済みだった（`face_s17b_human.blend`の手直し内容は、ハイライトの区画そのものではなく別の範囲だったため、そのまま持ち越すのは見送り）。ハイライトの丸2つを、輪郭が硬い円から、縁がなめらかなグラデーションになるよう塗り直した |
| 完了（続き） | ユーザー指示「1.2.3.4」（保留4項目に対応）。**③二重線の自己交差**: 調査したところ現在は0件（過去のS15c修正、または人間の手直しで解消済みだった。追加対応不要）。**④UV投影の歪み（首）**: 円柱投影で、首の背中側の継ぎ目（角度±π）をまたぐ面のUVが画面の端から端まで伸びていた（実際にワイヤーを描いて確認）。面ごとに角度を2π単位でunwrapして修正（`out/s18_1_face_uv_wire.png`で改善を確認、42面中40面は解消・2面は残存だが軽微）。**②素体の頂点グループ・基準点**: `Face`に`grp_eye`(119頂点・黒目中心から75mm以内)・`grp_nose`(7頂点)・`grp_mouth`(27頂点)を作成。`GUIDE`コレクションに基準点（`GUIDE_eye_center`・`GUIDE_nose_tip`・`GUIDE_mouth_center`）を配置。8/8 PASS。**①目の上下の頂点数（8=コーナー2+上6 / 下5）**: 調査の結果、眼窩の奥（R1・center）まで含めた大掛かりな改修が必要で、S13以降の黒目・まつ毛・眉・二重線・ハイライト全てが厳密な頂点対応で組まれているため、変更すると総崩れする恐れが高いと判断。**変更を見送ることを推奨**（差し戻し可）。`out/face_s17e_gen.blend`（上書き） |
| 完了（続き） | 顔は一通り完了。**髪の工程を開始**（ふさこ氏『髪の大ラフモデリング』第3回、20:55）。ただし今回のキャラの髪型は動画のツインテールではなく、ユーザー手配でAntigravityが生成した新しい下絵（`textures/human/chibi_hair_ref_{front,side,45}.png`、肩までのウェーブボブ＋一直線の前髪、茶色）に合わせる。下絵は既存の座標系（`front_px_to_xz`、COL0/ROW0/K）と整合済み（黒目中心の画素で検証）。方針: H01前髪→H02サイド〜後ろ髪→H03色、の順（ツインテールは無し） |
| 完了（続き） | **H01 前髪（1回目、廃案）**: 房ごとに細いリボンを7本、隙間を空けて並べた。ユーザー指摘「下絵を度外視している」で、実際の下絵は隙間のない一枚のシートで、房の違いは下端の高さのジグザグだけと判明。作り直し（下の行） |
| 完了（続き） | **H01 前髪（作り直し）**（10/10 PASS、`out/hair_h01_gen.blend`、ユーザーの確認待ち）: 中心線側の半分を、u=300〜512を6px間隔で36列×9行の格子（隙間なし）で作成。列ごとに、下絵の毛→肌境界（5pxの移動平均で単一画素ノイズのみ均す。房の高さの違いは残す）を下端、額の上部（行195）を上端にして、各行を個別に皮膚へレイキャスト。Solidifyで厚み、Mirror→Subdivision(1/2)。下端（毛先）にクリース1.0。**列と行を増やし、皮膚からのオフセットを11mmまで上げてようやく交差0に**（1枚の連続した面は、両端だけでなく格子全体が曲面に沿う必要があり、格子が粗いと弦が弧の内側に入って皮膚に埋まる） |
| 完了（続き） | **H01 前髪（3回目）**（8/8 PASS）: Mirror廃止・参考画像差し替え対応（詳細は上）。房の幅が足りず氷柱状に見える問題は未解決のまま、ユーザーに現状報告 |
| 完了（続き） | ユーザーが `out/hair_h01_human.blend` で、右目付近の長い房1つを手直しし「サンプル」として提示（幅約56-60mm、丸い先端、緩やかなカーブ、髪色は仮）。**H01 前髪（4回目、パラメータ調整）**（8/8 PASS、`out/hair_h01_gen.blend`）: サンプルに合わせてHALF_W 19mm→28mm、TIP_FRAC 40%→70%（先端を丸く）、断面の厚み(H_FRONT/H_BACK)を約1.8倍、膨らみ(BULGE)を19mm→6mmへ増加。隙間なく重なる丸みのある房になり、氷柱状の見た目が解消（ユーザー確認待ち） |
| 完了（続き） | **H01 前髪：人間が全て手作りで作り直した**（`out/hair_h01_human.blend`、2026-09-25）。`Hair_Bangs`から分離し、Planeから作り直して平面を拡張、最後に`E`→`Y`で厚み（立方体化）。房は`Hair_Bangs.001〜.008`の8個（各24〜40頂点、生成版は384頂点）。外側の房の一部だけMirrorで複製。旧`Hair_Bangs`（217頂点）は非表示で残存。**自動生成版（`steps/h01_bangs.py`）は不採用**。要整理: ①`.004〜.006`にスケール0.015〜0.016・位置オフセットが残っている（適用が必要）②`.001〜.003`のモディファイア順がSubdivision→Mirror（動画・手順書はMirror→Subdivision）③MirrorのClippingがOFF |
| 完了（続き） | **H02 サイド〜後ろ髪（試し、7/8 PASS、`out/hair_h02_gen.blend`、ユーザーの確認待ち）**: H01の手作り版から学んだ手法（幅広で薄い長方形断面4頂点×7駅の箱＋Subdivision(1)のみ、Mirror不使用、位置・回転・拡大は単位）で、`Hair_Back.001〜.012`の12房を生成。下絵の髪の外形（正面の半幅X(z)・側面の後ろY(z)）を楕円の帯とみなし、θ=-108°〜108°に振った房を頭の上(Z=0.27)から下端付近へ垂らす。頭・黒目との交差0（背面の房は最大36mm外へ押し出し）。**FAIL1件: 髪の外形からのはみ出し 後ろ40.7mm（背面の房を頭から逃がしたため）**。見た目の課題: 背中側で房の隙間から頭が見える、房が真っ直ぐで硬い（ウェーブ無し）、房の先端が揃いすぎ |
| 完了（続き） | **H02 作り直し（7/9 PASS、`out/hair_h02_gen.blend`、ユーザーの確認待ち）**: 指摘（頭頂が禿げ・変な隙間・毛先が丸い・ウェーブ不足）に対応。①頭の全方向6000本の光線で「髪に覆われない割合（禿げ）」を数値化: 主の房のみ 31.8% → 頭の曲面に沿った隙間埋めの房（`Hair_Fill.001〜.040`、上限40）を足して **8.1%**（ゲート2%未満は未達）。頭頂は覆えた（上面図で確認）②毛先を尖らせ、S字のウェーブ（左右・前後、房ごとに位相・振幅）と毛先の跳ね上げを付与 ③外形はみ出し: 頭で説明できない超過3頂点(最大21mm)が残る。**課題**: 隙間埋めが40房と多く、房が重なった雑然とした構造（ゲートは力業で満たす方式）。おでこの中央上に隙間埋めの房が小さく飛び出している。耳の裏〜首の後ろにまだ隙間 |
| 次 | （旧案）H02（サイド〜後ろ髪）は、H01と同じく「Planeを拡張→E Yで厚み→必要な所だけMirror」で人間が作り、Claudeは測定・検証・整理（変換の焼き込み、モディファイア順、交差チェック、UV・色）を担当する方針を提案中（ユーザー判断待ち） |
| 未決事項 | 口の高さ・前後位置の基準（下絵が3視点で食い違う。S03 は高さ=正面、前後=側面の輪郭）。顔の表面が出来上がる S05 以降で最終的に決まるので、口は暫定でよい |
| 学習メモ | `LEARNINGS.md`（S02 の補正量、下絵の食い違い、鼻は側面の輪郭を使う） |

## 動画（字幕で確認済み）
- 『Blenderでキャラクターモデル制作！01 | 顔のモデリング（前編）』ふさこ / 3D工房、36:22。字幕: `.agents/handoffs/fusako_01_subtitles_timestamped.txt`（+ `_raw.vtt`）
- 操作列の手順書 `.agents/handoffs/2026-09-21_fusako_head_modeling_01_steps.md` は**大枠のみ**信頼する。数値・コード例は無効（座標は下絵の測定、API は Blender 5.2 に合わせる）。細部は字幕で確認する
- 字幕で確認した S02 の根拠: Circle 頂点数8（05:45）、RX90、S/G、目尻を GY で後退（06:26）、Alt+クリック→E で押し出し＋拡大（06:39）、側面を見ながら全体を GY（07:02）。動画は目分量（「なんとなく」）で配置している

## S02 で得た教訓（測定ゲートの落とし穴）
- 側面の目の開口を最初「連結成分の bbox」で測って u 671..740 とした。虹彩の黒縁で分断され白目側だけを測っており、**ゲートは自分の定数と比べるだけで素通りした**。オーバーレイ画像を目視して発見。→ 現在は画像から毎回再測定する（虹彩右端＝黒画素の最右端、白目左端＝連続4画素以上の灰白）
- 黒線のアンチエイリアス縁は「灰白」に誤検出される。連続画素数で除外する
- ゲートは「作ったものが測った値と一致するか」の確認にすぎない。**見た目の妥当性はオーバーレイ画像で人が確認する**

## 履歴
- S01: 下絵は3枚とも 1024², Workbench 描画で元画像と平均差 0.0008、位置ズレ0px、左右反転なしを確認
- S02: 16頂点・8四角面（内輪8＋外輪8）、内輪が正面の開口（u262..420, v467..585）と側面の開口（u672..768）に一致、外輪は重心中心に1.3倍、目頭が前・目尻が後ろ。レビュー画像: `out/s02_{front,a45,side}_overlay.png`
