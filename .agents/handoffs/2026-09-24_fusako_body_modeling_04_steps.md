# ふさこ氏『体のモデリング』操作手順・トポロジー仕様書（第4回）

## 1. 動画メタデータ（公式事実）
- **正式タイトル**: 『Blenderでキャラクターモデル制作！04 | 体のモデリング〜初級から中級者向けチュートリアル〜』
- **URL**: https://www.youtube.com/watch?v=CgaTqJ3C-iI&list=PL7AyBndD_6pWbIRIb0KoO8ZnvsVQhLNLD&index=4
- **動画長**: 20分45秒（1245秒）
- **チャンネル**: ふさこ / 3D工房
- **アップロード日**: 2023年7月9日
- **字幕データ**:
  - VTT生データ: `z:\MeshCreator\.agents\handoffs\fusako_04_subtitles_raw.ja.vtt`
  - タイムスタンプ付きテキスト: `z:\MeshCreator\.agents\handoffs\fusako_04_subtitles_timestamped.txt`
- **重要シーン・スクリーンショット集（全32枚）**:
  - 保存フォルダ: [`z:\MeshCreator\docs\fusako_04_body_screenshots/`](file:///z:/MeshCreator/docs/fusako_04_body_screenshots/)
  - 画像カタログ: [`z:\MeshCreator\docs\fusako_04_body_screenshots/README.md`](file:///z:/MeshCreator/docs/fusako_04_body_screenshots/README.md)


---

## 2. チャプター構成一覧

| # | タイムスタンプ | チャプター名 | 開始プリミティブ | 主な対象パーツ | 概要 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **C1** | 00:00 - 02:30 | 胴体（体幹）のモデリング | `Cube` + Subsurf1適用 | 胸・ウエスト・骨盤・股下 | 立方体から球体化、X軸対称編集、底面押し出し2段で股下・お尻のベース作成 |
| **C2** | 02:30 - 10:30 | 足と腕のベースモデリング | `Cube`（45°回転） | 脚部・腕部（単体） | 上下面/前後面削除の4面筒、45度回転によるひし形断面化、E押し出し、Subsurf適用で8頂点ループ化 |
| **C3** | 10:30 - 15:48 | 胴体側の受け口（8頂点）準備 | 胴体メッシュ | 脇（肩口）・股関節（パンツライン） | 胴体の割り追加、統計オーバーレイ確認、側面4面インセット/押し出し、底面4面削除で8頂点開口部を作成 |
| **C4** | 15:48 - 20:45 | 腕・足の接合と首元の作成 | 統合メッシュ（`Ctrl + J`） | 肩接合・股関節接合・首元 | 腕: 頂点スナップ＋オートマージ、足: LoopTools Bridge、首: 天面4面削除＋E押し出しネックライン |

---

## 3. モデリング方針とトポロジー黄金律

### 核心方針: 「極小ポリゴン（4面筒）からの展開と8頂点の法則」
1. **手足の「45度回転ひし形断面」**:
   - 立方体の筒を `R Z 45`（足）または `R X 45`（腕）で45度回転させてひし形断面にする。
   - **メリット**: 正面・側面の最外殻に頂点が配置されるため、膝・肘・ふくらはぎの輪郭（シルエット）調整が「頂点1つの移動」で直感的に行える。
2. **「8頂点ループ整合」の法則**:
   - 4面筒に `Subdivision Surface` をかけると自動的に「8角形（8頂点ループ）」になる。
   - 胴体側の受け口も **「4面を抜いた8頂点ループ」** に設計することで、胴体と手足が **1対1の綺麗な四角面（Quad）のみで完全に破綻なく接合** できる。
3. **接合手法の柔軟な使い分け**:
   - **腕の接合**: 頂点スナップ（Vertex Snap）＋ 自動マージ（Auto Merge）で、胴体側の頂点を腕側の頂点にピタッと吸着結合。
   - **足の接合**: 胴体側の8頂点ループと足側の8頂点ループを選択し、アドオン `LoopTools` $\to$ `Bridge`（ブリッジ）で一発架橋。
4. **パンツ（水着）ラインのトポロジー**:
   - 股関節の付け根は、ハイレグ水着や下着のラインに沿った斜めのエッジ流（V字ライン）を作ることで、脚を曲げた時の変形破綻を防ぐ。

---

## 4. パーツ別詳細手順

### Part 1: 胴体（体幹）のモデリング (00:00〜02:30)
1. `Shift + A` $\to$ **メッシュ > 立方体（Cube）** を追加。
2. `Ctrl + 1` で `Subdivision Surface`（レベル1）を追加し、`Ctrl + A` でモディファイアを即時適用（丸みを帯びたベースキューブ化）。
3. `S` で縮小し、`G Z` で胸〜みぞおちの高さへ配置。
4. 編集モードで `X` 軸対称編集（ヘッダー右上の「X」アイコン、またはオートミラー）をON。
5. 下絵（正面・側面）を見ながらプロポーショナル編集（`O`）や `S X`, `S Y` で胸〜ウエストのくびれを大まかに調整。
6. アドオン **Auto Mirror** を実行（または手動で半分削除して Mirror モディファイア＋クリッピングON）。
7. **骨盤〜股下の押し出し**:
   - 胴体の一番下の底面を選択し、`E` で下方に押し出し（骨盤の広がり）。
   - さらに底面を選択してもう一度 `E` で押し出し、股下・お尻の最下部まで伸ばす。
   - 側面から見て、お尻の丸みと下腹部の傾斜を整流。

---

### Part 2: 足（脚部）のモデリング (02:30〜04:39, 07:10〜09:59)
1. `Shift + A` $\to$ **メッシュ > 立方体（Cube）** を追加。
2. 編集モードで **一番上と一番下の面を選択して `X` $\to$ 面を削除**（4面の筒にする）。
3. **`R Z 45` で45度回転**（ひし形断面化）。
4. `S Shift + Z` で水平方向の太さを絞り、`S` で全体スケールを下絵の太ももに合わせる。
5. 最下部のエッジループを選択し、`E` で足首付近まで一気に押し出し。
   - 内股気味に少し角度を傾ける。
6. `Ctrl + R` で膝周辺、ふくらはぎ周辺にループカットを追加。
   - 横から見て、膝がカクッと前に出るライン、ふくらはぎが後ろに膨らむラインを頂点移動で形成。
7. オブジェクトモードで足 $\to$ 胴体の順に複数選択し、`Ctrl + J` で統合（足にも胴体のMirrorが適用される）。
   ※後で個別にSubsurfをかけるため、必要に応じて編集モードで `L`（リンク選択） $\to$ `P` $\to$ `Selection` で再度分離。
8. 足に `Ctrl + 1`（Subdivision Surface レベル1）を追加。
   - 細くなりすぎるため、全選択 `A` $\to$ **`Alt + S`（法線方向に収縮/膨張）** で太さを下絵に合わせる。

---

### Part 3: 腕（腕部）のモデリング (04:40〜07:09, 09:59〜10:30)
1. `Shift + A` $\to$ **メッシュ > 立方体（Cube）** を追加。
2. 編集モードで **横面（前後面）を選択して `X` $\to$ 面を削除**。
3. **`R X 45` で45度回転**（ひし形断面化）。
4. `S` で縮小し、肩口の位置へ配置。
5. 端のエッジループを選択し、`E` で手首の位置まで一気に押し出し。
6. `Ctrl + R` で肘や前腕にループカットを追加し、膨らみを持たせる。
   - **ふさこ氏のTips**: 「一番太くなっているピーク位置が、上下（表裏）で全く同じ位置にあると単調で不自然に見える。あえて位置を前後にずらすことで人間らしい自然なカーブになる」。
7. 腕にも `Ctrl + 1`（Subdivision Surface レベル1）を追加し、全選択 `A` $\to$ `Alt + S` で太さを下絵に合わせる。

---

### Part 4: 胴体側の接続準備（8頂点の開口部作成） (10:30〜15:48)
1. **頂点数確認の準備**:
   - ビューポート右上の「ビューポートオーバーレイ」ドロップダウンを開き、**「統計（Statistics）」にチェック** を入れる。選択中の頂点数・面数が画面左上に常時表示される。
2. **腕の接続口（肩口）の作成**:
   - 胴体の側面にループカットを追加し、肩の付け根部分が「4面」のグリッドになるよう割りを整える。
   - この側面4面を選択 $\to$ **`E`（押し出し） $\to$ `S`（縮小）** で肩口の台座を作る。
   - 腕の断面に近づくよう位置を整え、先端の面を削除（開口部が **8頂点** になる）。
3. **足の接続口（股関節/パンツライン）の作成**:
   - 胴体下部にループカットを追加し、お尻と下腹部の間を水着やパンツのようなV字ラインに整流。
   - 胴体底面の股関節部分の **4面を選択して `X` $\to$ 面を削除**。
   - 統計オーバーレイで確認すると、開口部のループが正確に **8頂点** になっていることを確認。
   - 頂点の偏りがないよう、`G G`（頂点スライド）で周囲の面の間隔を均等に整える。

---

### Part 5: 腕・足の接合と首元の作成 (15:48〜20:45)
1. **モディファイアの確定**:
   - 接合前に、腕と足の `Subdivision Surface` モディファイアにマウスカーソルを合わせ、**`Ctrl + A` で適用** する（8角形のポリゴンメッシュに確定）。
2. **腕の接合（頂点スナップ＋自動マージ）**:
   - 胴体と腕を選択し、`Ctrl + J` で1つのオブジェクトに統合。
   - ヘッダーの **「スナップ」をON（スナップ先: 頂点）** にし、**「自動マージ（Auto Merge）」をON**。
   - 胴体側の肩口頂点を `G` で腕側の対応する頂点へドラッグすると、ピタッと吸着して自動的に一体化（全8頂点を順次結合）。
3. **足の接合（LoopTools Bridge）**:
   - 胴体と足を選択し、`Ctrl + J` で統合。
   - `Alt + 左クリック` で胴体側の股関節8頂点ループを選択、さらに `Shift + Alt + 左クリック` で足上端の8頂点ループを追加選択。
   - 右クリック $\to$ **LoopTools > Bridge（ブリッジ）** を実行（8頂点同士が綺麗な四角面で架橋される）。
   - ブリッジ部のねじれや歪みを `G` や `G G` でスムーズに整える。
4. **首元の作成（頭部接続準備）**:
   - 胴体天面の首の位置にある **中央寄りの4面を選択して `X` $\to$ 面を削除**。
   - 首の開口部エッジループを選択し、**`E`（押し出し） $\to$ `S`（縮小）** で首の立ち上がりパイプを作成。
   - 開口部ループを選択し、右クリック $\to$ **LoopTools > Relax（リラックス）** で綺麗な円形に整流。
   - 鎖骨・肋骨の膨らみ、脇の下のゆとりを微調整。
5. **仕上げ**:
   - オブジェクト名を `Body` 等にリネームして保存。

---

## 5. 必須テクニック＆ショートカット早見表

| ショートカット / 機能 | 操作名称 | ふさこ氏チュートリアルでの用途 |
| :--- | :--- | :--- |
| `R Z 45` / `R X 45` | 45度回転 | 手足のCubeをひし形断面にし、前後・側面の頂点でシルエット制御を容易にする |
| `Alt + S` | 法線方向に収縮/膨張 | Subsurf適用で極端に細くなった手足を、元の断面形状を保ったままふっくら太らせる |
| `Ctrl + A` (モディファイア上) | モディファイア適用 | 腕・足のSubsurfを確定させ、実体メッシュ（8頂点）にして胴体とトポロジー接合する |
| `Shift + Tab` | スナップON/OFF | 頂点スナップの切り替え |
| オートマージ (Auto Merge) | 自動頂点結合 | 頂点スナップで重なった頂点を自動的に溶接（マージ）する |
| `右クリック` $\to$ `LoopTools` $\to$ `Bridge` | ブリッジ架橋 | 胴体と足の8頂点ループ同士を四角面で綺麗に架橋する |
| `右クリック` $\to$ `LoopTools` $\to$ `Relax` | リラックス | ガタついた首元や開口部の頂点間隔を滑らかに均等化する |
| ビューポート統計 (Statistics) | 頂点数カウント | 接続予定の開口部ループが正確に「8頂点」になっているかを即座に検証する |

---

## 6. 本プロジェクト（AnimeFace / v05A）との連動・実装指針

1. **現在の進行状況との整合性**:
   - 先行して作成した `v05A_Gem_body_base.blend` は、まさに本動画の **「Part 1: 胴体（体幹）のモデリング」** を忠実に再現した状態である。
   - 次のステップ（`v05B` 以降）では、本手順書の **「Part 2: 手足の45度Cube」「Part 3: 8頂点ループ化」「Part 4 & 5: Bridge接合」** をそのまま適用することで、完全な破綻のない素体（ベースボディ）が完成する。
2. **頭部（AnimeFace / v04J）との接続規格**:
   - 本手順書の Part 5 で作成する首開口部は、`v04J`（またはAnimeFaceの顎下）の首シリンダーと頂点数（8または12）を揃えることで、将来の頭体統合時にワンタッチで接合可能となる。
